#!/bin/bash
# Ranix OS

#create the bootloader
make -f ./primary_bootloader/make_bootloader

#create kernel image
make -f ./kernel/make_kernel

#create floppy image
make -f ./floppy/make_floppy 

#run bochs emulator
bochs -f ./bochs/config

